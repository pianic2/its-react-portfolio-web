name: Quality

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  content-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Capture content validation output
        run: npm run content:validate > /tmp/content-validation.log 2>&1 || true

      - name: Upload content validation output
        uses: actions/upload-artifact@v4
        with:
          name: content-validation-diagnostic
          path: /tmp/content-validation.log
          if-no-files-found: error
          retention-days: 1

      - name: Enforce content validation gate
        run: npm run content:validate
